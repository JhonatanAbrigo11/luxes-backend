PORT=4000
NODE_ENV=development
JWT_SECRET=cambia-este-secreto-en-produccion
JWT_EXPIRES_IN=7d
# Varias URLs separadas por coma (dev: 5173, docker prod: 8080)
CORS_ORIGIN=http://localhost:5173,http://localhost:8080

# URL de conexión de base de datos PostgreSQL (local dev: localhost, docker dev: db)
DATABASE_URL="postgresql://luxes:luxes123@localhost:5432/luxes_dev?schema=public"

